[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = [
  "npm ci --omit=dev",
  "pip3 install playwright pyyaml openpyxl"
]

[phases.build]
cmds = [
  "python3 -m playwright install chromium --with-deps"
]

[start]
cmd = "node --dns-result-order=ipv4first src/app.js"
